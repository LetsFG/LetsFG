{
  "mcpServers": {
    "letsfg": {
      "command": "npx",
      "args": ["-y", "letsfg-mcp"],
      "env": {
        "LETSFG_API_KEY": "YOUR_API_KEY_HERE (optional — only needed for unlock/book)"
      }
    }
  }
}
