{
  "letsfg": {
    "command": "npx",
    "args": [
      "-y",
      "letsfg-mcp"
    ],
    "env": {
      "LETSFG_BEARER_TOKEN": "${LETSFG_BEARER_TOKEN}"
    },
    "description": "Flight and hotel search & booking for AI agents. Server-side engine covers every airline in the world with per-flight reliability history, plus real bookable hotel rates, refundable and non-refundable. Flights book for real: the fare is held on the connected card, a LetsFG booking agent buys the ticket, and the hold is captured only against a real PNR. ONE credential covers both: the card-backed token from the connect flow (put it in LETSFG_BEARER_TOKEN) or a Developer API key. Connect through the hosted MCP at https://letsfg.co/developers/api/mcp: the consent step opens letsfg.co/connect: one tap, no card (the card is asked for at the first booking, in a 0.00 Revolut setup, nothing charged). The token you get is card-backed and can book. Hotels use the same card: booking one holds the price and captures it only once the hotel confirms.",
    "homepage": "https://letsfg.co",
    "repository": "https://github.com/LetsFG/LetsFG"
  }
}
